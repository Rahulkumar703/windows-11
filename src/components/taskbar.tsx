import React from "react";

const Taskbar = () => {
  return (
    <footer className="relative z-10 p-1 flex items-center justify-between h-14 bg-background/60 backdrop-blur mt-auto w-full border-t">
      Taskbar
    </footer>
  );
};

export default Taskbar;
